# Database Configuration
# Format: postgresql://username:password@host:port/database_name
# Example for local PostgreSQL:
DATABASE_URL=postgresql://postgres:password@localhost:5432/happy_birthday_mate

# Security - IMPORTANT: Generate a secure random secret key (minimum 32 characters)
# Generate using: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Or use: openssl rand -hex 32
SECRET_KEY=your-secret-key-here-generate-a-secure-random-key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Firebase Admin SDK
# Option 1: For local development - Download from Firebase Console > Project Settings > Service Accounts > Generate New Private Key
# Save the JSON file in the backend folder as firebase-credentials.json
FIREBASE_CREDENTIALS_PATH=./firebase-credentials.json

# Option 2: For production (Render/cloud) - Paste entire JSON content as a single-line string
# FIREBASE_CREDENTIALS={"type":"service_account","project_id":"your-project",...}

# Payment Providers (Optional - leave empty if not using)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
PAYSTACK_SECRET_KEY=

# Flutterwave Payment Gateway
# Get your keys from: https://dashboard.flutterwave.com/settings/developers
FLUTTERWAVE_PUBLIC_KEY=
FLUTTERWAVE_SECRET_KEY=
FLUTTERWAVE_ENCRYPTION_KEY=  # Optional but recommended
FLUTTERWAVE_WEBHOOK_HASH=  # For webhook signature verification

# CORS - Add your frontend URLs here (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Google Gemini API (Optional - for AI-generated birthday messages)
# Get your API key from: https://aistudio.google.com/
# Add your API key here for production
GEMINI_API_KEY=

# Email Service Configuration (Optional - for custom email sending)
# If using Resend (recommended):
# RESEND_API_KEY=your_resend_api_key_here
# EMAIL_FROM=noreply@happybirthdaymate.com
# EMAIL_FROM_NAME=Happy Birthday Mate

# If using SendGrid instead:
# SENDGRID_API_KEY=your_sendgrid_api_key_here
# EMAIL_FROM=noreply@happybirthdaymate.com
# EMAIL_FROM_NAME=Happy Birthday Mate

